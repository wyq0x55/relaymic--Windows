bin/
.context/
.DS_Store

# 落地页（site/）
node_modules/
.wrangler/

# 凭证：任何形式都不进仓库
.dev.vars
.env
*.local

# 私有文档：内部策略、服务器地址、发帖草稿——绝不进公开仓库。
# 曾经因为 git add -A 误提交过一次，这条是补的锁。
docs/relaymic-private-docs/
*-private-docs/
# 本地临时产物：截图、一次性测试二进制等，不进仓库。
.tmp/
