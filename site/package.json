{
  "name": "relaymic-site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute relaymic --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute relaymic --local --file=./schema.sql",
    "waitlist": "wrangler d1 execute relaymic --remote --command \"SELECT created_at, email, country, ref FROM waitlist ORDER BY id DESC LIMIT 50\"",
    "waitlist:count": "wrangler d1 execute relaymic --remote --command \"SELECT date(created_at) AS day, ref, count(*) AS signups FROM waitlist GROUP BY day, ref ORDER BY day DESC\""
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
