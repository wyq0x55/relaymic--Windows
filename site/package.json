{
  "name": "relaymic-site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute relaymic --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute relaymic --local --file=./schema.sql",
    "waitlist": "node tools/waitlist.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
